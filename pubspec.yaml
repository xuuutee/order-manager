name: order_manager
description: 工作室订单管理系统 MVP
publish_to: 'none'
version: 1.0.6+6

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  provider: ^6.1.0
  go_router: ^14.0.0
  fl_chart: ^0.69.0
  intl: ^0.19.0
  url_launcher: ^6.3.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
